import type { Ticket } from "../../types";

interface Props {
  ticket: Ticket;
}

// Main Component
const TicketEditAccordion = ({ ticket }: Props) => {
  return <div></div>;
};

export default TicketEditAccordion;
